{
    "name": "@trex/cache",
    "version": "0.1.0",
    "description": "Snapshot-materialize a source-database schema into local DuckDB",
    "scripts": {
      "prepack": "make configure && make release && mv build/release/extension/*/*.trex . && ../extension-ci-tools/extension_signing/sign-if-key.sh"
    },
    "author": "",
    "license": "Apache-2.0",
    "trex": {
        "core": {
            "name": "cache"
        }
    }
}
