# Handbook FAQ

This page is contributed by a SECOND plugin (`@trex/memory-example2`) into
the SAME `handbook` memory that `@trex/memory-example` already declares —
proving two plugins can feed one shared brain. `mergeMemoryEntries` unions
this plugin's `extra` source into the existing `handbook` MemoryEntry
alongside `memory-example`'s `docs` source, since the source names are
distinct.
