{
  "imports": {
    "std/": "https://deno.land/std@0.224.0/"
  },
  "tasks": {
    "test": "deno test --allow-all test/",
    "coverage": "deno test --allow-all --coverage=coverage test/ && deno coverage coverage --lcov > coverage/lcov.info"
  }
}
