{
    "name": "@trex/webapi",
    "version": "0.1.0",
    "description": "OHDSI WebAPI embedded as a trex/DuckDB extension (webapi_start/stop/status)",
    "scripts": {
      "prepack": "make configure && make release && mv build/release/extension/*/*.trex . && ../extension-ci-tools/extension_signing/sign-if-key.sh"
    },
    "author": "",
    "license": "Apache-2.0",
    "trex": {
        "core": {
            "name": "webapi"
        }
    }
}
